# Fleet workspace v1 follow-up

Refines the first fleet workspace after the initial PR merge:

- ship selection starts at 0;
- 10 existing Scout «Vector» ships occupy 20 population (2 each);
- selected population is calculated separately from total occupied population;
- compact Max/Min controls;
- mission UI follows the Nemexia saved-page interaction pattern: select + icon grid + hover/focus description;
- missions: Transport, Espionage, Attack, Deployment, Colonization, Recycling, Gas extraction, Sun support, Space flight;
- removed temporary text mission strip.
