# Asterion web preview

The browser preview is deployed automatically from `main` by `.github/workflows/pages.yml`.

Expected public URL:

`https://ratoker-jpg.github.io/Asterion/`

The preview is intended for fast visual review of UI and gameplay screens. Windows desktop packaging remains handled separately by the CI workflow.
